// Given an array of numbers, use the map function to create a new array where each value is doubled.

const numbers = [1, 2, 3, 4, 5, 6, 7, 8];
let doubleNumbers = numbers.map((num) => num+num );
console.log(doubleNumbers);

